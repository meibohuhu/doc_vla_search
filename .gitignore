*.pyc
**/__pycache__/
.pytest_cache/*
.pydevproject

# IDE
.vscode/*

# Pip
*.egg-info

# files
*.log
*.jpg
*.pcd

# lsf
*.bsub

# temp
temp/*

# local model / training outputs
Qwen2.5-VL-3B-Instruct/
runs/sft/

# [mh 2026/07/22] dataset 是指向本机数据盘的 symlink（这台机器上是 /scratch/mh2803/vla），
# 每台机器的路径都不一样。原本它被 git 跟踪（上游指向 /data/autovla_data），
# 会导致本地路径被误提交、以及 checkout/stash 时把 symlink 改回去、训练读不到数据。
# 现在改为不跟踪：换机器时自己建软链即可 —— ln -s <你的数据目录> dataset
dataset

# [mh 2026/07/22] 训练进程的 pid 文件，纯本地临时产物
*.pid